This empty file tells GitHub Pages to publish the folder exactly as it is,
without running it through the Jekyll site generator first.
Keep it — it costs nothing and prevents surprises with folders whose names
start with an underscore.
